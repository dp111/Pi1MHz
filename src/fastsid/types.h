/* Minimal VICE types for FastSID. */
#ifndef VICE_TYPES_H
#define VICE_TYPES_H

#include <stdint.h>

typedef uint8_t BYTE;
typedef uint16_t WORD;
typedef uint32_t DWORD;
typedef int16_t SWORD;
typedef int32_t SDWORD;
typedef uint32_t CLOCK;

#endif
